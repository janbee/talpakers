import { useEffect, useState } from 'react';

const templateName = () => {
  const [data, setData] = useState(true);

  useEffect(() => {
  }, []);

  return { data };
};

export default templateName;
